import 'package:flutter/material.dart';

class BottomNavStatistics extends StatelessWidget {
  const BottomNavStatistics({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Statistics'),
      ),
    );
  }
}